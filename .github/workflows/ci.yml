name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  build-test-pack:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          global-json-file: global.json

      - name: Restore
        run: dotnet restore NetCid.sln --locked-mode --tl:off

      - name: Build
        run: dotnet build NetCid.sln -c Release --no-restore --tl:off

      - name: Test (Unit)
        run: dotnet test NetCid.Tests/NetCid.Tests.csproj -c Release --no-build --tl:off

      - name: Test (Integration)
        run: dotnet test NetCid.IntegrationTests/NetCid.IntegrationTests.csproj -c Release --no-build --tl:off

      - name: Dependency Vulnerability Scan (gated)
        run: |
          set -euo pipefail
          out="$(dotnet list NetCid.sln package --vulnerable --include-transitive)"
          echo "$out"
          if printf '%s\n' "$out" | grep -qiF 'has the following vulnerable packages'; then
            echo "::error::Vulnerable packages detected"
            exit 1
          fi

      - name: Pack
        run: dotnet pack NetCid/NetCid.csproj -c Release --no-build -o artifacts --tl:off

      - name: Upload Package Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: nupkg
          path: artifacts/*
